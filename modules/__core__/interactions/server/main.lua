-- M('command')
-- local utils = M('utils')

-- local Change = Command("change", "admin", _U('admin_command_spawn_prop'))
-- Change:addArgument("name", "string", "name")
-- Change:addArgument("value", "number", "number")
-- Change:setHandler(function(player, args)
--   if args.name and args.value then
--     emitClient("esx:interactions:change", player.source, args.name, args.value)
--   end
-- end)

-- Change:register()